"""Static (binary) expert placeholders.

The simulator-only benchmark does not bundle real ELF binaries by default.
If you populate data/fusionbench_sim/binaries/binaries.csv with paths, you can
extend these scripts to compute deterministic ELF/string features.
"""
